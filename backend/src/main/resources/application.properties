# Server Configuration
server.port=${PORT:8080}
spring.application.name=arribot-backend

# MongoDB Atlas Configuration (Cloud Database)
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://vit:12345@personal-projects.tc4hpkk.mongodb.net/arribot?retryWrites=true&w=majority}
spring.data.mongodb.database=arribot
spring.data.mongodb.auto-index-creation=true

# Gemini API Configuration
gemini.api.key=${GEMINI_API_KEY:AIzaSyC7XCxNWGPo8mYC14D8yahwgqHXyDYQD6c}
gemini.api.url=https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent
gemini.model=gemini-2.0-flash

# Groq Configuration (for Resume Analysis)
groq.api.key=${GROQ_API_KEY}
groq.api.url=https://api.groq.com/openai/v1/chat/completions
groq.model=${GROQ_MODEL:llama-3.3-70b-versatile}

# CORS Configuration
cors.allowed.origins=${CORS_ORIGINS:http://localhost:5173,http://localhost:3000}

# Logging
logging.level.com.arribot=INFO
logging.level.org.springframework.data.mongodb=DEBUG

# SendGrid Email Configuration
sendgrid.api.key=${SENDGRID_API_KEY:your-sendgrid-api-key-here}
sendgrid.from.email=${SENDGRID_FROM_EMAIL:vit1122334@gmail.com}
sendgrid.from.name=ArriBot

# JWT Configuration
jwt.secret=${JWT_SECRET:ArriBot2025SecretKeyForJWTTokenGenerationPleaseKeepThisSecure}
jwt.expiration=86400000

# File Upload Configuration
file.upload.dir=${FILE_UPLOAD_DIR:./uploads/resumes}
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB
spring.servlet.multipart.enabled=true

# AI Usage Limits (per user per day)
ai.limits.chat.daily=30
ai.limits.flashcards.daily=10
ai.limits.quiz.daily=5
ai.limits.summary.daily=10

# Rate Limiting (per user per minute)
ai.ratelimit.requests.per.minute=5
ai.ratelimit.window.seconds=60

# Global AI Control
ai.enabled=true
